# Django Admin Panel с Unfold

Современная админ-панель для управления PostgreSQL базой данных Node.js/Prisma проекта.

## 🚀 Быстрый старт

### Шаг 1: Создание виртуального окружения

```powershell
# Перейти в папку admin_panel
cd admin_panel

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows PowerShell)
.\venv\Scripts\Activate.ps1

# Или для CMD
# .\venv\Scripts\activate.bat
```

### Шаг 2: Установка зависимостей

```powershell
pip install -r requirements.txt
```

### Шаг 3: Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните данные:

```powershell
cp .env.example .env
```

Отредактируйте `.env`:
```
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=5432
TELEGRAM_BOT_TOKEN=your_bot_token
DJANGO_SECRET_KEY=your-secret-key-here
```

### Шаг 4: Генерация моделей из существующей БД

```powershell
# Сгенерировать модели из базы данных
python manage.py inspectdb > core/models_generated.py

# Просмотреть сгенерированный файл и скопировать нужные модели в core/models.py
```

⚠️ **ВАЖНО:** Убедитесь, что в Meta классе каждой модели указано `managed = False`, чтобы Django не создавал миграции и не ломал Prisma схему!

### Шаг 5: Применение миграций Django

```powershell
# Создать миграции для Django системных таблиц (auth, sessions и т.д.)
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser
```

### Шаг 6: Запуск сервера

```powershell
python manage.py runserver 8080
```

Откройте http://localhost:8080/admin/

## 📁 Структура проекта

```
admin_panel/
├── admin_panel/          # Настройки проекта
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── core/                 # Основное приложение
│   ├── admin.py          # Регистрация моделей в админке
│   ├── models.py         # Модели (из inspectdb)
│   └── actions.py        # Кастомные действия (broadcast)
├── .env                  # Переменные окружения
├── manage.py
└── requirements.txt
```

## 🔧 Особенности

- **Unfold Theme** — современный UI на базе Tailwind CSS
- **Read-only режим** — Django не управляет миграциями (`managed = False`)
- **Broadcast** — массовая рассылка через Telegram Bot API
- **Поиск** — быстрый поиск по telegram_id и другим полям
